Model‑by‑build table: GPU + model + workload mapping

This table maps each GPU, each model, and each workload sweep to Build 1–7.

| Build     | Name                                      | Target GPU                       | Model                                                              | Workload sweep / objective                                                                                                                                                                                             |
| --------- | ----------------------------------------- | -------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1         | vLLM Performance Triage                   | RTX 4090 24 GB                   | Qwen2.5‑7B‑Instruct                                                | Baseline, prompt‑length sweep (512–8192), output‑length sweep (128–1024), request‑rate sweep (1–32 RPM). Goal: isolate TTFT vs ITL vs queue vs KV‑pressure. wheynelau+1                                                |
| 2         | Prefill, Decode, and KV Behavior          | RTX 4090 24 GB                   | Qwen2.5‑7B‑Instruct                                                | Long‑context (1K–16K), long‑output (128–2048), mixed prefill/decode contention, prefix‑caching behavior, KV‑pressure sweeps. Goal: show how prefill‑dominant vs decode‑dominant loads behave. pliops+1                 |
| 3         | vLLM Optimization Playbook                | RTX 4090 24 GB                   | Qwen2.5‑7B‑Instruct                                                | Batching sweeps, max_num_batched_tokens, max_num_seqs, gpu_memory_utilization, max_model_len, quantization swings (FP16 vs INT4). Goal: derive tuning‑knob‑to‑symptom decision rules. vllm+1                           |
| 4         | Inference Routing Control Plane           | RTX 4090 24 GB (2× backends)     | Qwen2.5‑7B‑Instruct                                                | Homogeneous short‑requests, mixed short‑/long‑requests, slow‑backend, overloaded‑backend, and stale‑metric scenarios. Goal: compare round‑robin, least‑active, queue‑aware, latency‑aware routing. vllm+1              |
| 5         | Distributed Inference Strategy            | MI350X (ROCM)                    | Qwen2.5‑32B‑Instruct, Qwen2.5‑72B‑Instruct, Llama‑3.1‑70B‑Instruct | Single‑GPU baseline, replicated‑instances, tensor‑parallel, and mixed‑sweep runs (latency vs throughput vs tail‑latency under 1K–32K context). Goal: “When does replicas pay off versus TP‑overhead on MI350X?” vllm+1 |
| 6         | Training‑to‑Serving Infrastructure        | MI350X (ROCM, optional flagship) | Qwen2.5‑32B‑Instruct / 72B‑Instruct (LoRA/QLoRA‑tuned)             | Fine‑tune 32B/72B adapters on MI350X, package, then measure adapter‑load latency, memory overhead, and quality‑vs‑cost versus 7B‑baseline. Goal: gate‑based adapter‑promotion rules. bentoml+1                         |
| 7         | Reliability and Operations Lab            | RTX 4090 24 GB                   | Qwen2.5‑7B‑Instruct                                                | Backend crash, slow‑backend, KV‑pressure, long‑context‑spike, mixed‑workload, stale‑metrics, bad‑routing scenarios. Goal: incident‑diagnosis and runbook‑style failure‑modes. dev+1                                    |
| 7 (extra) | Reliability + large‑model / large‑context | MI350X (ROCM)                    | Qwen2.5‑32B‑Instruct, Qwen2.5‑72B‑Instruct                         | Same failure‑injects as Build 7, but on 32B/72B‑class models and 1K–32K contexts. Goal: show how failure‑signatures differ on large‑model / large‑context stacks. pliops+1                                             |
